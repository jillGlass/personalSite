import React from 'react'

export class Main extends Component {
    render() {
        return (
            <div>
                This is the main page and it should hold the grid.
            </div>
        )
    }
}

export default Main
